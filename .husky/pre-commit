#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🧪 Running npm test (build check)..."
npm test || { echo "❌ Build failed! Fix errors before committing."; exit 1; }

echo "✅ Build successful! Proceeding with commit."
exit 0
